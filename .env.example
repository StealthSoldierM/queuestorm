# QueueStorm environment variables (example — never commit real secrets).
# Copy to .env for local development and set in Render dashboard for deploys.

# Future: API key for optional LLM fallback. Not required for the rules-based path.
# OPENAI_API_KEY=
